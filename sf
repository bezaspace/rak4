#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/front end"

if [ ! -d node_modules ]; then
  echo "[sf] Installing frontend dependencies with bun install..."
  bun install
fi

echo "[sf] Starting frontend at http://localhost:5173"
exec bun run dev
