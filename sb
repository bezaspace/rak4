#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/back end"

if [ ! -d .venv ]; then
  echo "[sb] Setting up backend environment with uv sync..."
  uv sync
fi

echo "[sb] Starting backend at http://localhost:8000"
exec uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
